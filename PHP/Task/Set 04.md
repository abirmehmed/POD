# Advanced PHP: Set 4 (Final)
## Performance Tuning, Profiling, FFI & Engine-Level Optimization

## 📘 10 Conceptual Questions
1. **Profiling Overhead & Methodologies:** How do profilers like Xdebug and Blackfire actually work under the hood? Explain the difference between sampling profilers (low overhead, statistical) and tracing profilers (high overhead, exact), and why you should never leave Xdebug enabled in production.
2. **Reading Flame Graphs:** What is a flame graph, and how do you read it to distinguish between CPU-bound bottlenecks (wide blocks) and I/O-bound bottlenecks (deep, narrow stacks waiting on DB/network)?
3. **OPcache Preloading (PHP 7.4+):** How does `opcache.preload` eliminate the file system I/O cost of `require`/`include` on every request? What are the strict constraints of preloading (e.g., no reloading without a PHP-FPM restart, dependency resolution order)?
4. **Foreign Function Interface (FFI) (PHP 7.4+):** What is FFI, and how does it allow PHP to call C functions directly without writing a full C extension? What are the security risks (e.g., memory corruption, segfaults) and performance trade-offs of crossing the PHP/C boundary?
 each request? Explain how PSR-4 autoloading scales poorly, and why `composer dump-autoload --optimize` (classmap) is a mandatory production step.
6. **The True Cost of N+1 Queries:** Why does executing 100 tiny `SELECT` queries take exponentially longer than 1 query with a `JOIN` or `IN()` clause, even if the total data returned is identical? Explain network round-trips, PDO context switching, and ORM hydration overhead.
7. **String Building at the Engine Level:** Why is `$str .= $chunk` in a large loop often less efficient than accumulating chunks in an array and using `implode()`? Explain how PHP's Zval engine handles string reallocation and memory copying.
8. **Writing PHP Extensions (Zend API):** When is it actually justified to drop down to C and write a custom PHP extension (using `ext_skel`)? What kinds of problems (e.g., heavy cryptography, custom data structures, SIMD operations) warrant this extreme measure?
9. **JIT Tuning & Pitfalls:** Beyond enabling it, how do you tune `opcache.jit_buffer_size` and the JIT mode (e.g., `1255` tracing vs `1205` function)? Why can JIT actually *decrease* performance and increase memory usage for short-lived CLI scripts or highly dynamic code?
10. **Real User Monitoring (RUM) vs. Synthetic Profiling:** Why does profiling a single request in a local development environment often fail to reveal production bottlenecks? How do distributed tracing and APM tools (like New Relic, Datadog, or Elastic APM) provide a more accurate picture of system health?

---

## 🛠️ 10 Practical Tasks

| # | Task | Success Criteria |
|---|------|------------------|
| 1 | **Xdebug Profiling & Analysis**<br>Install Xdebug. Configure `xdebug.mode=profile` and `xdebug.start_with_request=yes`. Run a slow script, generate a `cachegrind` file, and analyze it using Webgrind or QCacheGrind to identify the top 3 most expensive functions. | You can successfully generate and read a profiling file. You pinpoint the exact function consuming the most "inclusive" CPU time. |
| 2 | **Autoloader Optimization Benchmark**<br>Take a large Laravel/PHP project. Measure the bootstrap time (e.g., `time php artisan route:list`). Run `composer dump-autoload --optimize --classmap-authoritative`. Measure the time again. | You observe a measurable decrease in bootstrap time (often 20-50ms+) due to eliminated file system `stat` calls. |
| 3 | **OPcache Preload Configuration**<br>Create a `preload.php` script that uses `opcache_compile_file()` to load your core domain models and traits. Configure `php.ini` (`opcache.preload=/path/to/preload.php`). Restart PHP-FPM and verify via `opcache_get_status()` that files are preloaded. | `opcache_get_status()['preload_statistics']` shows your files are loaded into shared memory. Bootstrap time drops further. |
| 4 | **Basic FFI Implementation**<br>Use PHP's `FFI` extension to call a standard C library function (e.g., `strlen` from `libc.so.6` or `msvcrt.dll`). Pass a PHP string, get the length back, and compare it to PHP's native `strlen()`. | FFI successfully bridges PHP and C. You understand the syntax for defining C signatures in PHP. |
| 5 | **Generate a Flame Graph**<br>Use a tool like `phpspy`, `Tideways`, or Blackfire CLI to profile a web request. Generate a visual flame graph. Identify a "plateau" (wide block) representing a slow operation (e.g., a large `foreach` or DB query). | You produce a valid flame graph image. You can explain what the X-axis (time) and Y-axis (call stack depth) represent. |
| 6 | **N+1 Eradication via Profiling**<br>Intentionally write an endpoint that triggers an N+1 query (e.g., looping 50 orders and accessing `$order->user->name`). Profile it. Fix it using Eager Loading (`with('user')`). Profile again. | The "Before" profile shows 51+ database calls. The "After" profile shows exactly 2 calls. Wall time drops significantly. |
| 7 | **String Concatenation Benchmark**<br>Write a script that builds a 100,000-item string using `.= ` in a loop. Write a second version that pushes to an array and uses `implode()`. Measure both `microtime(true)` and `memory_get_peak_usage(true)`. | The `implode` version demonstrates lower peak memory usage and/or faster execution due to avoided Zval reallocations. |
| 8 | **Before/After Blackfire (or APM) Report**<br>Profile a specific MultiMart endpoint. Apply one meaningful optimization (e.g., adding a missing index, caching a config, or removing a loop). Profile it again. Document the "Wall Time" and "CPU Time" delta. | Clear, data-driven proof of optimization. You can articulate *why* the CPU time or I/O wait time decreased. |
| 9 | **Compile a "Hello World" C Extension**<br>Use the `php-src/ext_skel.php` script to generate a basic extension skeleton. Add a simple `hello_world()` function. Compile it (`phpize`, `./configure`, `make`), add it to `php.ini`, and verify it works via CLI. | `php -m` lists your custom extension. `php -r "echo hello_world();"` executes the C code successfully. |
| 10| **Production Profiling Runbook**<br>Write a `PROFILING.md` guide for your team. Document how to safely profile a *single* production request (e.g., using a specific `X-Profile: 1` header with Blackfire/Tideways) without degrading performance for other users. | Document includes exact configuration steps, safety warnings, and how to retrieve the profile link. |
