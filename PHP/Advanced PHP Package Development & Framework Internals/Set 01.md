# Advanced PHP Package Development & Framework Internals: Set 1
## Laravel Internals, The Service Container & Advanced Design Patterns

## 📘 10 Conceptual Questions
1. **Service Container Resolution Mechanics:** How does Laravel's IoC container actually resolve a class when you call `app()->make()` or type-hint it in a constructor? Explain the difference between `bind`, `singleton`, `scoped`, and `instance`, and when to use each.
 a Custom Service Provider:** What is the exact execution order of Service Providers (`register` vs. `boot`)? Why is it a critical anti-pattern to resolve other services inside the `register` method, and how do you properly defer provider loading?
3. **Contextual Binding:** How do you tell the Service Container to inject `DatabaseLogger` when resolving `CheckoutService`, but `FileLogger` when resolving `AuditService`, even though both depend on the `Logger` interface?
4. **The Macroable Trait:** How does Laravel allow you to dynamically add methods to core classes (like `Collection` or `Response`) at runtime? Explain the underlying PHP mechanics (`__call` and closures bound to `$this`).
5. **The Pipeline Pattern:** How does Laravel’s `Pipeline` class work under the hood? Explain how it chains multiple "pipes" (classes with a `handle` method) to process data sequentially, and why this is superior to a massive, monolithic service class.
 spatie/laravel-query-builder` or `laravel/telescope`) structure their code to be framework-agnostic at the core, but seamlessly integrate with Laravel via a Service Provider?
9. **Resolving Circular Dependencies:** What happens when Class A requires Class B in its constructor, and Class B requires Class A? How does Laravel detect this, and what architectural patterns (e.g., Setter Injection, Event Dispatching, or a Mediator) resolve it?
10. **The Laravel Request Lifecycle:** Trace a request from `public/index.php` to the final HTTP Response. Name the 5-7 critical stages (e.g., HTTP Kernel, Service Providers, Router, Middleware, Controller, Pipeline, Response) and what happens at each.

---

## 🛠️ 10 Practical Tasks

| # | Task | Success Criteria |
|---|------|------------------|
| 1 | **Contextual Binding Implementation**<br>Create an interface `PaymentGateway`. Create two implementations: `StripeGateway` and `PayPalGateway`. Use contextual binding (`$this->app->when(CheckoutService::class)->needs(...)`) to inject the correct gateway based on the consuming class. | Resolving `CheckoutService` yields Stripe. Resolving `RefundService` yields PayPal. No hardcoded `new` instances or manual container calls in the classes. |
| 2 | **Build a Custom Macro**<br>Use the `Macroable` trait to add a custom method to Laravel’s `Collection` class (e.g., `->toCsv()` or `->chunkByColumn()`). Write a test proving it works identically to native collection methods. | The method is available globally on all Collections. It is chainable and returns the expected data type. |
| 3 | **Implement the Pipeline Pattern**<br>Refactor a multi-step process (e.g., processing an imported CSV row: Validate → Transform → Save → Notify) into a Laravel Pipeline. Create separate "Pipe" classes for each step. | The main service method is reduced to ~5 lines calling `Pipeline::send($data)->through([...])->then(...)`. Each pipe is independently testable. |
| 4 | **Create a Custom Artisan Command**<br>Build a command that interacts with the user (e.g., `php artisan multimart:setup`). Use `$this->confirm()`, `$this->choice()`, and `$this->withProgressBar()` to guide the user through a multi-step configuration process. | Command handles invalid input gracefully. Progress bar accurately reflects task completion. Output is clean and color-coded. |
| 5 | **Deferred Service Provider**<br>Create a Service Provider for a heavy, rarely-used service (e.g., a PDF generator). Implement the `$defer = true` property and the `provides()` method. Verify it does not load on every request. | `php artisan provider:clear` and tracing shows the provider is only booted when the specific service is actually resolved from the container. |
| 6 | **Custom Validation Rule Object**<br>Create a custom rule class (e.g., `ValidPhoneNumber` or `UniquePerTenant`) implementing `Illuminate\Contracts\Validation\ValidationRule`. Use it in a Form Request. | The rule can be reused across multiple Form Requests. It provides a clear, translatable error message. It is unit-tested with passing and failing data. |
| 7 | **Tagged Services Resolution**<br>Bind multiple implementations of a `ReportGenerator` interface to the container and tag them all as `reports`. Create a service that type-hints `iterable $generators` to receive all of them at once. | The service successfully loops through and executes all registered report generators without knowing their specific class names. |
| 8 | **Middleware with Terminate Logic**<br>Create a custom middleware that records the start time of a request. Implement the `terminate($request, $response)` method to calculate the total execution time and log it *after* the response has been sent to the browser. | The browser receives the response instantly. The log entry is written in the background without adding to the user's perceived latency. |
| 9 | **Facade Creation & Testing**<br>Create a custom class `MultiMart\Analytics\Tracker`. Create a Facade `TrackerFacade` that provides static access to it. Write a test using `TrackerFacade::shouldReceive()` to mock the underlying instance. | `Tracker::track('event')` works in the app. The test successfully intercepts the call without hitting the real analytics service. |
| 10| **Map the Request Lifecycle**<br>Write a markdown document or draw a diagram tracing a single API request through Laravel. Include: `index.php`, `Bootstrap`, `Service Providers (register/boot)`, `Router`, `Middleware (global/route)`, `Controller`, and `Kernel::terminate`. | The document accurately reflects the actual source code flow of Laravel. You can explain *why* each step exists. |
